@model AspMVCEcomerce.Models.PlanetModel;

<h1>@Model.Name</h1>

<div class="card w-50">
    <img src="~/planet/@(Model.Name).jpg" alt="">
    <div class="card-body">
        <div class="card-text">
            @Model.Content
        </div>
    </div>
    <a class="card-link" asp-action="Index">Các Hành Tinh</a>
</div>

